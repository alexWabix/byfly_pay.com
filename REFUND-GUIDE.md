# Руководство по возврату платежей

## Описание

Система поддерживает возврат (refund) платежей, выполненных через терминалы Kaspi. Возврат привязан к **конкретной операции** (partial_payment), а не ко всей транзакции.

## Как это работает

### 1. Клиент может оплачивать несколькими способами
- Одна транзакция может содержать несколько операций оплаты
- Каждая операция может быть выполнена разным способом (Gold, Red, Кредит, Рассрочка)
- Каждая операция может быть выполнена на разном терминале

### 2. Возврат операции (согласно документации Kaspi Smart POS)

**ВАЖНО:** Возврат выполняется строго по [документации Kaspi](https://guide.kaspi.kz/cdn/content/pay/product/documents/Kaspi%20POS/Smart-POS-Dokymentatsia-po-integratsii.pdf)

**Требования Kaspi для возврата:**
```
GET /v2/refund?method={method}&amount={amount}&transactionId={transactionId}
Headers:
  accesstoken: {token}
```

**Обязательные параметры:**
- `method` - способ оплаты: **qr**, **card**, или **alaqan**
- `amount` - сумма возврата
- `transactionId` - ID оригинальной транзакции от терминала
- `accesstoken` - токен доступа к терминалу (в заголовках)

При отмене конкретной операции система:
1. **Извлекает данные оплаты** из `payment_details` (JSON):
   - Определяет способ оплаты (qr или card)
   - Получает terminal_transaction_id
2. **Инициирует возврат на терминале Kaspi** согласно документации:
   - Вызывает `/v2/refund` с параметрами method, amount, transactionId
   - Передает accesstoken в заголовках
3. **Получает processId** от терминала
4. **Считывает QR код подтверждения** с камеры над терминалом
5. **Ждет завершения** операции возврата (до 60 сек)
6. **Обновляет запись** в `partial_payments` (is_refunded = 1)
7. **Пересчитывает итоги транзакции**:
   - Обновляет `paid_amount` (сколько оплачено)
   - Обновляет `actual_amount_received` (сколько получено чистыми)
   - Обновляет `remaining_amount` (остаток к оплате)
   - Меняет статус транзакции (pending/partially_paid/paid)

## Использование в админке

### Отмена операции

1. Откройте транзакцию в разделе **"История транзакций"**
2. В секции **"💳 История оплат"** найдите нужную операцию
3. Нажмите кнопку **"🔄 Отменить оплату"** под операцией
4. Подтвердите действие
5. Система выполнит возврат на терминале и обновит данные

### Что происходит после возврата

**Визуально:**
- Операция становится полупрозрачной
- Появляется красный значок **"🔄 ВОЗВРАТ ВЫПОЛНЕН"**
- Дата возврата отображается под значком

**В базе данных:**
```sql
-- Поля добавляются в partial_payments:
is_refunded = 1
refund_amount = 5050.00
refund_terminal_id = 1
refund_process_id = "abc123"
refund_qr_code = "https://..."
refund_transaction_id = "kaspi_txn_456"
refund_details = {...}
refunded_at = "2025-12-26 12:00:00"
refunded_by_admin_id = 1
```

**Пересчет транзакции:**
- Если отменены **все** операции → статус `pending`
- Если отменена **часть** операций:
  - Если остаток <= 10 ₸ → статус `paid`
  - Если остаток > 10 ₸ → статус `partially_paid`

## API Endpoint

```
POST /api/admin/partial-payments/{id}/refund
```

**Headers:**
```
Authorization: Bearer {jwt_token}
```

**Response (Success):**
```json
{
  "success": true,
  "message": "Возврат успешно выполнен",
  "data": {
    "refund_result": {
      "terminal_id": 1,
      "process_id": "abc123",
      "qr_data": "https://pay.kaspi.kz/...",
      "refund_transaction_id": "kaspi_txn_456",
      "refund_amount": 5050,
      "details": {...}
    },
    "transaction_status": "pending",
    "total_paid": 0,
    "total_received": 0,
    "remaining": 5000
  }
}
```

**Response (Error):**
```json
{
  "success": false,
  "message": "Возврат уже был произведен ранее"
}
```

## Миграция базы данных

При первом деплое выполните миграцию:

```bash
mysql -u payments -p payments < database/migration-refunds.sql
```

Или через phpMyAdmin скопируйте содержимое файла `database/migration-refunds.sql`.

## Ограничения

1. ✅ **Можно отменить** только операции, выполненные через терминал Kaspi
2. ❌ **Нельзя отменить** операцию, которая уже была отменена
3. ⚠️ **Требуется** доступность терминала (он должен быть online)
4. ⏱️ **Таймаут** операции возврата: 60 секунд

## Логирование

Все операции возврата логируются:
- PHP error_log: `REFUND: ...`
- Детали сохраняются в `partial_payments.refund_details` (JSON)

## Проверка терминала

Перед возвратом убедитесь что терминал доступен:
- Зайдите в **"Терминалы"**
- Нажмите **"Проверить статус"** для нужного терминала
- Статус должен быть **"online"** 🟢

## Безопасность

- Возврат может выполнить **только авторизованный админ**
- ID админа сохраняется в `refunded_by_admin_id`
- Требуется JWT токен с валидной сессией

## Примеры использования

### Сценарий 1: Полный возврат

**До возврата:**
- Оригинальная сумма: 5 000 ₸
- Оплачено: 5 050 ₸ (Kaspi Gold)
- Статус: `paid`

**После возврата операции #1:**
- Оплачено: 0 ₸
- Статус: `pending`
- Клиент может оплатить заново

### Сценарий 2: Частичный возврат

**До возврата:**
- Оригинальная сумма: 10 000 ₸
- Операция #1: 5 050 ₸ (Gold)
- Операция #2: 5 700 ₸ (Кредит)
- Статус: `paid`

**После возврата операции #2:**
- Оплачено: 5 000 ₸ (чистыми)
- Остаток: 5 000 ₸
- Статус: `partially_paid`
- Клиент должен доплатить 5 000 ₸

### Сценарий 3: Ошибочный платеж кредитом

**Проблема:**
- Клиент выбрал Gold (1%), но оплатил Кредитом (14%)
- Образовалась недоплата из-за разницы комиссий

**Решение:**
1. Отменить операцию с кредитом
2. Клиент оплачивает заново через Gold
3. Все суммы сходятся

## Поддержка

Все вопросы направляйте администратору ByFly Travel.

