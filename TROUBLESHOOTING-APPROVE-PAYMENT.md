# Решение проблем с подписанием платежей

## 🔍 Диагностика

### Шаг 1: Проверьте миграцию БД

Выполните на сервере:
```bash
mysql -u payments -p2350298aweA payments < database/check-approval-tokens.sql
```

**Что проверить:**
- Есть ли поле `approval_token` в таблице `payment_approvals`
- Есть ли токены в записях (не NULL)
- Статус записей (должен быть 'pending' для тестирования)

### Шаг 2: Откройте страницу с debug панелью

URL: `https://byfly-pay.com/approve-payment?token=ВАШ_ТОКЕН`

**Debug панель (правый верхний угол) покажет:**
```
Loading: false
Error: null или текст ошибки
Success: false
Payment: loaded или null
Approval: loaded или null
```

### Шаг 3: Проверьте Console

Откройте Console в DevTools. Должны быть логи:

**✅ Если всё работает:**
```
🚀 ApprovePayment component mounted
Route query: {token: "..."}
🔑 Token found: xxx
🔍 Loading payment with token: xxx
✅ Full API Response: {...}
✅ Response data: {...}
📋 Payment loaded: {...}
👤 Approval loaded: {...}
```

**❌ Если ошибка:**
```
❌ Error loading payment: ...
Error response: {...}
```

### Шаг 4: Проверьте Network

**Запрос:** `GET /api/approvals/payment-by-token/{token}`

**Статус:** Должен быть **200 OK**

**Response должен содержать:**
```json
{
  "success": true,
  "data": {
    "payment": {
      "id": 123,
      "title": "...",
      "amount": "...",
      ...
    },
    "approval": {
      "id": 456,
      "status": "pending",
      ...
    }
  }
}
```

### Шаг 5: Тестовая страница

Откройте: `https://byfly-pay.com/test-approve-payment.html?token=ВАШ_ТОКЕН`

1. Токен должен автоматически подставиться
2. Нажмите "Получить платеж"
3. Должен показать JSON с данными

## 🐛 Возможные проблемы и решения

### Проблема 1: "Payment: null, Approval: null"

**Причина:** API не возвращает данные

**Решение:**
1. Проверьте что токен существует в БД:
   ```sql
   SELECT * FROM payment_approvals WHERE approval_token = 'ВАШ_ТОКЕН';
   ```
2. Выполните миграцию если поле approval_token отсутствует
3. Сгенерируйте токены если их нет

### Проблема 2: "Error: Неверная или истекшая ссылка"

**Причина:** Токен не найден в БД

**Решение:**
```sql
-- Сгенерировать токены для всех записей
UPDATE payment_approvals 
SET approval_token = MD5(CONCAT(id, payment_id, admin_id, UNIX_TIMESTAMP(), RAND()))
WHERE approval_token IS NULL;
```

### Проблема 3: "Error: Платеж не найден"

**Причина:** В таблице payment_approvals есть запись, но платежа в outgoing_payments нет

**Решение:**
```sql
-- Проверить связь
SELECT pa.id, pa.payment_id, op.id as outgoing_id
FROM payment_approvals pa
LEFT JOIN outgoing_payments op ON pa.payment_id = op.id
WHERE pa.approval_token = 'ВАШ_ТОКЕН';
```

### Проблема 4: Страница белая, но в Console логов нет

**Причина:** Компонент не монтируется

**Решение:**
1. Проверьте что файл ApprovePayment.vue в деплое
2. Проверьте что роут добавлен в router/index.js
3. Жесткая перезагрузка: Ctrl+Shift+R

### Проблема 5: API возвращает 401 Unauthorized

**Причина:** Публичные endpoints проверяются после авторизации

**Решение:**
Убедитесь что в `api/endpoints/approvals.php` публичные endpoints идут ПЕРВЫМИ (до проверки JWT токена)

## 📋 Чеклист проверки после деплоя:

- [ ] Миграция выполнена (`migration-approval-tokens.sql`)
- [ ] Поле `approval_token` добавлено в `payment_approvals`
- [ ] Токены сгенерированы (не NULL)
- [ ] Папка `uploads/documents` создана и доступна
- [ ] Публичные endpoints в approvals.php ДО проверки авторизации
- [ ] Роут `/approve-payment` добавлен с `meta: { public: true }`
- [ ] Router guard проверяет `to.meta.public` ПЕРВЫМ
- [ ] Жесткая перезагрузка страницы (Ctrl+Shift+R)

## 🧪 Быстрый тест:

```bash
# На сервере в MySQL
USE payments;

# Получить тестовый токен
SELECT approval_token 
FROM payment_approvals 
WHERE status = 'pending' AND approval_token IS NOT NULL 
LIMIT 1;

# Скопируйте токен и откройте в браузере:
# https://byfly-pay.com/test-approve-payment.html?token=ТОКЕН

# Нажмите "Получить платеж" - должен показать JSON
```

## 📞 Если ничего не помогло:

1. Проверьте PHP error log: `tail -f /var/log/php_errors.log`
2. Проверьте Nginx/Apache error log
3. Проверьте что .htaccess работает (mod_rewrite включен)
4. Проверьте что путь к API правильный

---

**Важно:** После КАЖДОГО изменения делайте жесткую перезагрузку браузера (Ctrl+Shift+R)!




