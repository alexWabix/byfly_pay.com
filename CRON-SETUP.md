# Настройка CRON задач для ByFly Payment Center

## 📋 Автоматические задачи

### 1. Проверка терминалов (каждый час)

**Что делает:**
- Проверяет все активные терминалы Kaspi
- Тестирует доступность камер
- Проверяет deviceinfo терминалов
- Сбрасывает зависшие операции (>4 минут)
- Отправляет SMS админам при проблемах

**Настройка:**

```bash
# Откройте crontab
crontab -e

# Добавьте строку (проверка каждый час):
0 * * * * php /var/www/html/api/cron/check-terminals.php >> /var/www/html/api/logs/cron-terminals.log 2>&1
```

**Или каждые 30 минут:**
```bash
*/30 * * * * php /var/www/html/api/cron/check-terminals.php >> /var/www/html/api/logs/cron-terminals.log 2>&1
```

**Проверка работы:**
```bash
# Запустите вручную для теста
php /var/www/html/api/cron/check-terminals.php

# Смотрите логи
tail -f /var/www/html/api/logs/cron-terminals.log
```

---

## 📊 Логи

Все cron задачи пишут логи в:
```
/var/www/html/api/logs/cron-terminals.log
```

Формат лога:
```
=== KASPI TERMINALS CHECK ===
Started at: 2025-12-24 10:00:00

Checking 7 terminals...

[1/7] Checking Terminal #1 (Терминал 1)...
  ✅ ONLINE - Терминал доступен

[2/7] Checking Terminal #2 (Терминал 2)...
  ❌ OFFLINE - Камера недоступна

=== SUMMARY ===
Total terminals: 7
Online: 6
Offline: 1
Problems detected: 1

=== SENDING ALERTS ===
  ✅ SMS sent to +77780021666

=== COMPLETED ===
Finished at: 2025-12-24 10:00:15
```

---

## 📱 SMS Уведомления

При обнаружении проблем отправляется SMS всем супер-админам:

```
⚠️ Kaspi Terminals Alert

Обнаружены проблемы с терминалами:

• Терминал 2: Камера недоступна
• Терминал 5: Терминал недоступен (HTTP 0)

Время: 24.12.2025 10:00

Проверьте терминалы: https://byfly-pay.com/terminals
```

---

## 🔧 Устранение неполадок

### Если cron не работает:

1. **Проверьте права:**
```bash
chmod +x /var/www/html/api/cron/check-terminals.php
```

2. **Проверьте путь к PHP:**
```bash
which php
# Используйте полный путь в cron, например:
# /usr/bin/php /var/www/html/api/cron/check-terminals.php
```

3. **Проверьте что cron запущен:**
```bash
sudo systemctl status cron
# или
sudo systemctl status crond
```

4. **Посмотрите системные логи cron:**
```bash
sudo tail -f /var/log/syslog | grep CRON
```

---

## 💡 Дополнительные задачи (опционально)

### Очистка старых логов (раз в неделю):
```bash
0 2 * * 0 find /var/www/html/api/logs -name "*.log" -mtime +30 -delete
```

### Очистка старых токенов (ежедневно):
```bash
0 3 * * * mysql -u payments -p2350298aweA payments -e "DELETE FROM admin_tokens WHERE expires_at < NOW()"
```

### Очистка старых SMS кодов (ежедневно):
```bash
0 4 * * * mysql -u payments -p2350298aweA payments -e "DELETE FROM sms_codes WHERE expires_at < NOW() OR is_used = 1"
```

---

**После настройки cron терминалы будут автоматически мониториться!** ✅





