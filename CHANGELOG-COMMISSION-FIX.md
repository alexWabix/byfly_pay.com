# Changelog - Исправление расчета комиссий

## [2.1] - 2025-12-27

### Исправлено 🐛

#### 1. Ошибка при ручной установке статуса "Оплачено"
**Проблема:** `SQLSTATE[42S22]: Column not found: 1054 Unknown column 'event_type' in 'field list'`

**Решение:**
- ✅ Исправлено в `api/endpoints/admin.php` (строки 784-793)
- ✅ Заменено `event_type` → `action`
- ✅ Заменено `event_data` → `request_data`
- ✅ Добавлены все необходимые поля для логирования

#### 2. Расхождения в расчете комиссий
**Проблема:** Комиссия считалась по-разному при создании платежа и при обработке

**Решение:**
- ✅ Создан единый метод `calculateCommission()` в `Transaction.php`
- ✅ Гарантирует идентичный расчет везде
- ✅ Используется при создании (`create()`)
- ✅ Используется при обработке (`processKaspiPayment()`)
- ✅ Правильное округление до 2 знаков

#### 3. Жестко закодированные проценты в CRON
**Проблема:** В CRON скриптах использовались фиксированные значения 14% и 1%

**Решение:**
- ✅ Исправлено в `api/cron/check-active-payments.php`
- ✅ Исправлено в `api/cron/recalculate-loan-commissions.php`
- ✅ Проценты теперь берутся из базы данных (`payment_methods`)
- ✅ Единообразие с основной логикой

### Добавлено ✨

#### 1. Единый метод расчета комиссий
```php
/**
 * ЕДИНЫЙ метод расчета комиссии для всех случаев
 * Гарантирует идентичный расчет при создании платежа и при обработке
 */
private function calculateCommission($paymentMethod, $amount, $paymentType = null)
```

**Преимущества:**
- ✅ Один код для всех случаев
- ✅ Нет расхождений
- ✅ Легко поддерживать
- ✅ Автоматическое округление

#### 2. Миграция базы данных
Файл: `database/fix-commission-calculation.sql`

**Проверяет и добавляет поля:**
- `terminal_operation_id` - ID операции терминала
- `actual_payment_method` - Реальный способ оплаты
- `actual_amount_received` - Реально полученная сумма
- `needs_additional_payment` - Флаг необходимости доплаты
- `payment_mismatch` - Несоответствие способа оплаты
- `client_ip`, `client_device`, `client_browser` - Информация о клиенте
- `payment_started_at` - Время начала оплаты

#### 3. Документация
- ✅ `FIX-COMMISSION-DEPLOY.md` - Инструкция по деплою
- ✅ `CHANGELOG-COMMISSION-FIX.md` - Этот файл

### Изменено 🔄

#### Логика расчета комиссий

**Было:**
```php
// При создании - одна логика
$commissionAmount = $amount * ($commissionPercent / 100);
$amount = $amount + $commissionAmount;

// При обработке - другая логика  
if ($paymentMethod['has_credit']) {
    $commissionPercent = $paymentMethod['credit_commission_percent'];
}
// ... разная логика
```

**Стало:**
```php
// Везде один метод
$calculation = $this->calculateCommission($paymentMethod, $amount, $paymentType);
$commissionAmount = $calculation['commission'];
$totalAmount = $calculation['total'];
```

#### CRON скрипты

**Было:**
```php
// Жестко закодированные проценты
$bankCommission = $actualAmount * 0.14; // Всегда 14%
$bankCommission = $actualAmount * 0.01; // Всегда 1%
```

**Стало:**
```php
// Берем из базы данных
$paymentMethodInfo = $db->fetch(
    "SELECT commission_percent, credit_commission_percent, installment_commission_percent 
     FROM payment_methods WHERE code = ?",
    [$actualMethod]
);
$commissionPercent = $paymentMethodInfo['commission_percent'];
$bankCommission = round($actualAmount * ($commissionPercent / 100), 2);
```

## Как это влияет на работу системы

### Для администраторов
- ✅ Больше нет ошибки при ручной установке статуса "Оплачено"
- ✅ Расчеты комиссий всегда корректны
- ✅ Нет расхождений в суммах

### Для разработчиков
- ✅ Код стал проще и понятнее
- ✅ Один метод вместо нескольких
- ✅ Легче поддерживать

### Для бизнеса
- ✅ Точные расчеты комиссий
- ✅ Нет потерь из-за ошибок округления
- ✅ Можно легко изменить проценты в БД

## Миграция

### Требуется выполнить:
```bash
mysql -u payments -p payments < database/fix-commission-calculation.sql
```

### Безопасность:
- ✅ Использует `ADD COLUMN IF NOT EXISTS` - безопасно запускать повторно
- ✅ Не изменяет существующие данные
- ✅ Только добавляет недостающие поля

## Тестирование

### Чек-лист:
- [ ] Ошибка "event_type" не появляется
- [ ] Статус "Оплачено" устанавливается без ошибок
- [ ] Комиссии считаются одинаково при создании и обработке
- [ ] CRON берет проценты из БД
- [ ] Все поля в БД существуют

### Сценарии тестирования:

#### 1. Ручная установка статуса
```
1. Найти транзакцию со статусом partially_paid
2. Нажать "Отметить как оплачено"
3. Проверить: статус изменился на paid, ошибки нет
```

#### 2. Создание платежа
```
1. Создать платеж через API на 10,000 ₸ (Kaspi Gold)
2. Проверить: amount = 10,100 ₸ (комиссия 1%)
3. Проверить: commission_amount = 100 ₸
```

#### 3. Обработка платежа
```
1. Инициировать платеж через терминал
2. Проверить: сумма на терминале = сумме при создании
3. Проверить: нет расхождений
```

#### 4. CRON скрипт
```bash
php api/cron/check-active-payments.php
# Проверить в логах: "bank takes X%:" где X из БД
```

## Откат изменений

Если нужно откатить:

```bash
# 1. Восстановить БД
mysql -u payments -p payments < backup_before_commission_fix_XXXXXX.sql

# 2. Вернуть старые файлы из git
git checkout HEAD~1 -- api/endpoints/admin.php
git checkout HEAD~1 -- api/includes/Transaction.php
git checkout HEAD~1 -- api/cron/check-active-payments.php
git checkout HEAD~1 -- api/cron/recalculate-loan-commissions.php
```

## Заметки

### Важно ⚠️
- Все проценты комиссий теперь хранятся и берутся из таблицы `payment_methods`
- Для изменения процентов используйте UPDATE в БД, а не меняйте код
- Метод `calculateCommission()` - это единственное место расчета комиссий

### Рекомендации 💡
- После деплоя проверьте текущие проценты в БД
- Убедитесь, что они соответствуют бизнес-требованиям
- При добавлении новых способов оплаты обязательно указывайте проценты

## Контакты

При вопросах обращаться к администратору ByFly Travel.

---
**Версия:** 2.1  
**Дата:** 27.12.2025  
**Статус:** ✅ Готово к деплою





