# Деплой системы управления банками

## 📋 Что было добавлено:

### Новая функциональность:

1. **🏦 Управление банками**
   - Создание/редактирование/удаление банковских счетов
   - Поддержка множества стран и валют
   - Редактирование баланса вручную (с автоматической корректировкой)

2. **💰 Операции по счетам**
   - Приход средств
   - Расход средств
   - История всех операций
   - Автоматический пересчет баланса

3. **📊 Dashboard со статистикой**
   - Общий баланс в тенге (конвертация всех валют)
   - Баланс по странам
   - Баланс по валютам с эквивалентом в ₸
   - Приход/расход за сегодня

4. **🔗 Интеграция с платежами**
   - Выбор банка при отметке платежа как оплаченный
   - Автоматическое списание с баланса
   - Создание транзакции расхода
   - История связанных платежей

## 🗄️ Миграция базы данных:

**ВАЖНО:** Выполните миграции в правильном порядке!

```bash
# На сервере выполните:
cd /path/to/your/site

# 1. Токены для подписания (если еще не выполнена)
mysql -u payments -p2350298aweA payments < database/migration-approval-tokens.sql

# 2. Дополнительные документы (если еще не выполнена)
mysql -u payments -p2350298aweA payments < database/migration-payment-documents.sql

# 3. Система банков (НОВАЯ)
mysql -u payments -p2350298aweA payments < database/migration-banks-system.sql
```

### Что создает миграция банков:

**Таблицы:**
- `banks` - банковские счета
- `bank_transactions` - история операций
- Добавляет поле `bank_id` в `outgoing_payments`

**Стандартные банки:**
- 💳 Kaspi Bank KZ (KZT) - 4400430199704070
- 🏦 Halyk Bank KZ (KZT)
- 💵 Forte Bank KZ (USD)
- 🏦 Kapital Bank AZ (AZN)
- 🏦 Ipak Yuli Bank UZ (UZS)
- 💰 Наличные KZT, USD, AZN

## 📁 Новые файлы:

### Backend:
- `api/endpoints/banks.php` - API для управления банками

### Frontend:
- `src/views/Banks.vue` - страница управления банками
- `src/views/Dashboard.vue` - обновлен с балансами

### Database:
- `database/migration-banks-system.sql` - миграция

## 🚀 Инструкция по деплою:

### 1. Загрузите deploy.zip на сервер

```bash
scp deploy.zip user@byfly-pay.com:/tmp/
```

### 2. На сервере распакуйте

```bash
cd /var/www/html  # или ваш корень сайта
unzip -o /tmp/deploy.zip
```

### 3. Выполните миграции

```bash
mysql -u payments -p2350298aweA payments < database/migration-banks-system.sql
```

### 4. Проверьте создание таблиц

```bash
mysql -u payments -p2350298aweA payments -e "SELECT * FROM banks;"
```

Должно показать 8 стандартных банков.

### 5. Жесткая перезагрузка браузера

**Ctrl + Shift + R** (очистка кеша)

## 📊 Использование:

### Страница "Банки":

1. **Посмотреть список всех счетов**
   - Таблица со всеми банками
   - Группировка по странам

2. **Добавить новый банк**
   - Кнопка "➕ Добавить банк"
   - Заполните форму
   - Укажите начальный баланс

3. **Редактировать банк**
   - Кнопка ✏️ в строке банка
   - **МОЖНО ИЗМЕНИТЬ БАЛАНС** - создастся корректировка

4. **Добавить операцию**
   - Кнопка ➕ в строке банка
   - Выберите тип: Приход или Расход
   - Укажите сумму и описание

5. **Посмотреть историю**
   - Кнопка 📊 в строке банка
   - Показывает все операции

6. **Удалить банк**
   - Кнопка 🗑️ в строке банка
   - Нельзя удалить если есть привязанные платежи

### Dashboard:

**Общий баланс:**
- Показывает сумму всех счетов в тенге
- Конвертация по актуальному курсу

**По странам:**
- 🇰🇿 Казахстан: суммы по валютам
- 🇦🇿 Азербайджан: суммы по валютам
- и т.д.

**По валютам:**
- ₸ KZT: сумма + эквивалент в ₸
- $ USD: сумма + эквивалент в ₸
- ₼ AZN: сумма + эквивалент в ₸

**За сегодня:**
- 📈 Приход (по всем валютам)
- 📉 Расход (по всем валютам)

## ⚠️ Возможные проблемы:

### "Banks endpoint not found"

**Решение:** Проверьте что файл `api/endpoints/banks.php` существует

```bash
ls -la /var/www/html/api/endpoints/banks.php
```

Если файла нет - распакуйте архив заново.

### Dashboard пустой

**Причины:**
1. Миграция не выполнена
2. Таблица `banks` пустая
3. Курсы валют не загружены

**Решение:**
```sql
-- Проверьте таблицы
SELECT * FROM banks;
SELECT * FROM exchange_rates;

-- Если banks пустая, выполните миграцию заново
```

### Нельзя удалить банк

Если есть сообщение "Нельзя удалить банк с привязанными платежами":

```sql
-- Найдите платежи
SELECT id, title FROM outgoing_payments WHERE bank_id = X;

-- Отвяжите или удалите платежи
UPDATE outgoing_payments SET bank_id = NULL WHERE bank_id = X;
```

## 📝 Примеры операций:

### Пополнение счета:
```
Тип: Приход
Сумма: 1000000
Описание: Поступление от клиента
```

### Снятие со счета:
```
Тип: Расход
Сумма: 50000
Описание: Снятие наличных
```

### Корректировка баланса:
```
При редактировании банка измените поле "Текущий баланс"
Автоматически создастся транзакция "Корректировка баланса"
```

---

**Версия:** 2.2.0  
**Дата:** 26.12.2025  
**ByFly Travel Payment Center**




