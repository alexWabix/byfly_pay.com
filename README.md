# ByFly Travel Payment Center

Универсальная платежная система для приема оплат через различные платежные системы с фокусом на терминалы Kaspi.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Технологии](#технологии)
- [Установка](#установка)
- [Настройка](#настройка)
- [API документация](#api-документация)
- [Интеграция Kaspi](#интеграция-kaspi)
- [Разработка](#разработка)

## 🎯 Описание

ByFly Payment Center - это полнофункциональная платежная система, которая позволяет:

- Принимать оплаты через различные платежные системы
- Управлять терминалами Kaspi с автоматическим считыванием QR кодов
- Обрабатывать платежи с учетом комиссий, кредитов и рассрочек
- Отслеживать все транзакции в реальном времени
- Интегрировать платежи в любые приложения через REST API

## ✨ Возможности

### Админ панель

- ✅ Авторизация по SMS через smsc.kz
- ✅ Управление администраторами с гибкими правами доступа
- ✅ Создание источников и генерация API токенов
- ✅ История всех транзакций с фильтрами
- ✅ Настройка способов оплаты и комиссий
- ✅ Управление терминалами Kaspi

### Платежные системы

**Казахстан:**
- Kaspi Gold (1% комиссия)
- Kaspi Red (1% комиссия)
- Kaspi Кредит (14% комиссия)
- Kaspi рассрочка 0-0-12 (14% комиссия)
- Kaspi рассрочка 0-0-24 (14% комиссия)

**Готовность к расширению:**
- Home Bank
- Halyk Bank
- Click (Узбекистан)
- Payme (Узбекистан)
- Интеграция с TipTop Pay для карт, Apple Pay, Google Pay

### API функционал

- Создание платежей
- Обработка через различные методы оплаты
- Проверка статуса в реальном времени
- Webhook уведомления
- Частичная оплата разными способами

## 🛠 Технологии

**Backend:**
- PHP 7.4+
- MySQL 5.7+
- REST API
- JWT авторизация

**Frontend:**
- Vue.js 3
- Vue Router
- Pinia (State Management)
- Vite
- Axios

**Интеграции:**
- smsc.kz (SMS)
- Kaspi POS терминалы
- Камеры для считывания QR кодов

## 📦 Установка

### Требования

- PHP 7.4 или выше
- MySQL 5.7 или выше
- Node.js 16+ (для сборки фронтенда)
- Apache/Nginx веб-сервер

### Быстрая установка

1. **Клонируйте или скачайте проект:**

```bash
git clone <repository-url>
cd payment_byfly
```

2. **Установите зависимости и соберите проект:**

```bash
npm install
chmod +x build.sh
./build.sh
```

3. **Загрузите созданный архив на сервер:**

Скрипт создаст файл `byfly-payment-center-YYYYMMDD-HHMMSS.zip`

4. **На сервере распакуйте архив:**

```bash
unzip byfly-payment-center-*.zip
```

5. **Инициализируйте базу данных:**

```bash
php api/init.php
```

6. **Настройте веб-сервер** (см. раздел Настройка)

## ⚙️ Настройка

### База данных

Отредактируйте `api/config.php`:

```php
define('DB_HOST', 'localhost');
define('DB_PORT', 3306);
define('DB_NAME', 'payments');
define('DB_USER', 'payments');
define('DB_PASS', '2350298aweA');
```

### SMS сервис (smsc.kz)

```php
define('SMS_LOGIN', 'Byfly2024');
define('SMS_PASSWORD', '2350298aweA!@');
```

### Apache конфигурация

Файл `.htaccess` уже настроен. Убедитесь, что модуль `mod_rewrite` включен:

```bash
sudo a2enmod rewrite
sudo systemctl restart apache2
```

### Nginx конфигурация

```nginx
server {
    listen 80;
    server_name byfly-pay.com;
    root /var/www/byfly-payment-center;
    index index.html;

    # Frontend routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API routes
    location /api {
        try_files $uri $uri/ /api/index.php?$query_string;
        
        location ~ \.php$ {
            fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
        }
    }
}
```

## 📚 API документация

### Авторизация

Все запросы к API требуют токен авторизации:
- **Публичные endpoints** (создание платежей): `X-API-Token` header
- **Админские endpoints**: `Authorization: Bearer {token}` header

### Публичные эндпоинты

#### 1. Создание платежа

```http
POST /api/payment/init
X-API-Token: your_api_token
Content-Type: application/json

{
  "amount": 10000,
  "currency": "KZT",
  "description": "Оплата за тур",
  "customer_phone": "+77001234567",
  "customer_email": "customer@example.com",
  "webhook_url": "https://your-site.com/webhook"
}
```

**Ответ:**

```json
{
  "success": true,
  "data": {
    "transaction_id": "ABC123DEF456...",
    "amount": 10000,
    "currency": "KZT",
    "status": "pending",
    "expires_at": "2024-01-01 15:00:00"
  }
}
```

#### 2. Обработка через Kaspi

```http
POST /api/payment/{transaction_id}/kaspi
X-API-Token: your_api_token
Content-Type: application/json

{
  "payment_method": "kaspi_gold"
}
```

**Доступные методы:**
- `kaspi_gold`
- `kaspi_red`
- `kaspi_credit`
- `kaspi_installment_12`
- `kaspi_installment_24`

**Ответ:**

```json
{
  "success": true,
  "data": {
    "transaction_id": "ABC123DEF456...",
    "amount": 10100,
    "original_amount": 10000,
    "commission_amount": 100,
    "commission_percent": 1,
    "payment_url": "https://kaspi.kz/pay/...",
    "qr_code": "https://kaspi.kz/pay/...",
    "terminal_id": 1
  }
}
```

#### 3. Проверка статуса

```http
GET /api/payment/{transaction_id}/status
X-API-Token: your_api_token
```

**Ответ:**

```json
{
  "success": true,
  "data": {
    "transaction_id": "ABC123DEF456...",
    "status": "paid",
    "amount": 10100,
    "paid_amount": 10100,
    "remaining_amount": 0
  }
}
```

**Возможные статусы:**
- `pending` - Ожидание оплаты
- `processing` - В обработке
- `paid` - Оплачено
- `partially_paid` - Частично оплачено
- `cancelled` - Отменено
- `failed` - Ошибка

#### 4. Отмена платежа

```http
POST /api/payment/{transaction_id}/cancel
X-API-Token: your_api_token
```

### Webhook уведомления

При успешной оплате система отправит POST запрос на указанный `webhook_url`:

```json
{
  "transaction_id": "ABC123DEF456...",
  "status": "paid",
  "amount": 10100,
  "paid_amount": 10100,
  "currency": "KZT",
  "description": "Оплата за тур",
  "metadata": {...},
  "paid_at": "2024-01-01 14:30:00"
}
```

**Headers:**
```
Content-Type: application/json
X-Webhook-Signature: sha256_hmac_signature
```

### Админские эндпоинты

#### Авторизация

```http
POST /api/auth/send-code
Content-Type: application/json

{
  "phone": "+77780021666"
}
```

```http
POST /api/auth/verify
Content-Type: application/json

{
  "phone": "+77780021666",
  "code": "123456"
}
```

#### Источники

- `GET /api/admin/sources` - Список источников
- `POST /api/admin/sources` - Создать источник
- `PUT /api/admin/sources/{id}` - Обновить источник
- `DELETE /api/admin/sources/{id}` - Удалить источник

#### Терминалы

- `GET /api/admin/terminals` - Список терминалов
- `POST /api/admin/terminals` - Добавить терминал
- `PUT /api/admin/terminals/{id}` - Обновить терминал
- `DELETE /api/admin/terminals/{id}` - Удалить терминал
- `GET /api/admin/terminals/{id}/status` - Проверить статус

#### Транзакции

```http
GET /api/admin/transactions?status=paid&date_from=2024-01-01&limit=100
Authorization: Bearer {token}
```

## 🖥️ Интеграция Kaspi

### Принцип работы

1. Система выбирает свободный терминал
2. Отправляет запрос на терминал для инициации платежа
3. Камера над терминалом считывает QR код
4. Генерируется ссылка для клиента
5. Каждую секунду проверяется статус платежа
6. При успешной оплате отправляется webhook

### Настройка терминалов

В админ панели → Терминалы Kaspi:

- **IP адрес**: `http://109.175.215.40` или `http://188.127.40.70`
- **Порт**: 130-140 (зависит от терминала)
- **ID камеры**: 1-8
- **URL камеры**: `http://109.175.215.40:3000/qr/{camera_id}`

### Текущие терминалы

| Порт | Камера | Статус |
|------|--------|--------|
| 130  | 4      | ✅     |
| 131  | 3      | ✅     |
| 132  | 8      | ✅     |
| 133  | 7      | ✅     |
| 136  | 5      | ✅     |
| 138  | 1      | ✅     |
| 140  | 2      | ✅     |

### Расчет комиссий

- **Kaspi Gold/Red**: 1% комиссии
- **Kaspi Кредит/Рассрочка**: 14% комиссии

⚠️ **Важно:** Если клиент выбрал Gold, но оплатил в кредит - система покажет недостачу и предложит доплатить разницу.

## 💻 Разработка

### Локальная разработка

1. **Установите зависимости:**

```bash
npm install
```

2. **Запустите dev сервер:**

```bash
npm run dev
```

3. **Настройте локальную БД:**

```bash
php api/init.php
```

4. **Откройте браузер:**

```
http://localhost:3000
```

### Структура проекта

```
payment_byfly/
├── api/                    # Backend API
│   ├── config.php         # Конфигурация
│   ├── index.php          # Точка входа API
│   ├── init.php           # Инициализация БД
│   ├── includes/          # Классы
│   │   ├── Database.php
│   │   ├── Auth.php
│   │   ├── KaspiTerminal.php
│   │   └── Transaction.php
│   └── endpoints/         # API endpoints
│       ├── auth.php
│       ├── payment.php
│       └── admin.php
├── src/                   # Frontend Vue.js
│   ├── main.js
│   ├── App.vue
│   ├── router/
│   ├── stores/
│   ├── views/
│   ├── components/
│   └── services/
├── database/
│   └── schema.sql         # SQL схема
├── dist/                  # Собранный фронтенд
├── package.json
├── vite.config.js
├── build.sh              # Скрипт сборки
└── README.md
```

### Добавление новой платежной системы

1. Добавьте запись в таблицу `payment_methods`
2. Создайте класс-обработчик в `api/includes/`
3. Добавьте endpoint в `api/endpoints/payment.php`
4. Обновите фронтенд для отображения

## 🔐 Безопасность

- ✅ JWT токены для админов
- ✅ API токены для источников
- ✅ SMS авторизация
- ✅ Prepared statements для SQL
- ✅ CORS настройки
- ✅ Webhook signature verification
- ✅ IP ограничения для источников

## 📞 Поддержка

**Первый вход:**
- Телефон: `+7 778 002 16 666`
- Получите SMS код
- Войдите в админ панель

**URL админ панели:**
```
https://byfly-pay.com/
```

## 📝 Лицензия

ByFly Travel Payment Center © 2024

---

**Разработано для ByFly Travel**
